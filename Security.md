**Security Problems**

the tool have some security problems such as modifying shell commands and since it records EVERYTHING that was in the 
terminal, any sensitive file thats read by cat for example will have it stored.

and the file names allow / * . its not a big problem but worth mentioning

so if you use it correctly nothing should be dangerous.

i will work on fixing these in the future.
